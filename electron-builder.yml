appId: com.schoolees.ai-usage
productName: AI Usage
directories:
  buildResources: build
  output: dist
files:
  - out/**
  - resources/**
  - package.json
asarUnpack:
  - resources/**
# Stable, space-free asset names so electron-updater's latest.yml matches what GitHub serves.
artifactName: ai-usage-setup-${version}.${ext}
publish:
  provider: github
  owner: Schoolees
  repo: ai-usage
# Flipped inside the packaged electron.exe. Off, the app's exe cannot double as a general Node
# interpreter (RunAsNode) or take code through NODE_OPTIONS / --inspect.
# Asar integrity validation is left off until it has been tried on an installed Windows build: if
# the embedded hash is missing or wrong, the app refuses to start.
electronFuses:
  runAsNode: false
  enableNodeOptionsEnvironmentVariable: false
  enableNodeCliInspectArguments: false
  onlyLoadAppFromAsar: true
win:
  target:
    - target: nsis
      arch: [x64]
  icon: build/icon.ico
nsis:
  # The same wizard as SchooleesPulse: an install-location page and a finish page with "Run".
  # Unlike SchooleesPulse it stays per-user. AI Usage reads the signed-in user's CLI logins, and a
  # per-machine install would ask for admin rights on every automatic update. build/installer.nsh
  # skips the "who is this for" page that a per-user assisted installer would otherwise show.
  oneClick: false
  perMachine: false
  allowToChangeInstallationDirectory: true
  createDesktopShortcut: true
  createStartMenuShortcut: true
  shortcutName: AI Usage
  runAfterFinish: true
